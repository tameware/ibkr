{
  "ibkr": {
    "client_id": 2,
    "account": "",
    "reconnect": true,
    "reconnect_delay_seconds": 5.0
  },

  "contract": {
    "exchange": "SMART"
  },

  "strategy": {
    "base_qty": 100,
    "min_order_size": 10,
    "//": "Max shares per buy order (capped by max_position - position)",
    "max_position": 300,
    "//": "Never hold more than this many shares (hard inventory cap)",
    "max_gross_shares": 5000,
    "//": "Daily share-turn cap (buy + sell fills)",
    "daily_volume_target": 400,
    "//": "Shares to buy and to sell per day; behind-schedule sides quote more aggressively",
    "min_profit_per_share": 0.03,
    "//": "Sells are floored at avg_cost + round-trip commission + this (never sell at a loss)",
    "commission_per_share": 0.005,
    "//": "Estimated commission per share; counted twice per round trip in the sell floor",
    "min_spread": 0.10,
    "//": "Only quote when NBBO spread is at least this wide",
    "max_spread": 4.00,
    "//": "Reject NBBO when spread exceeds this (stale/erroneous one-sided quotes)",
    "mid_delta": 0.02,
    "//": "Self-trade guard half-width vs mid (buys capped below, sells floored above)",
    "quote_refresh_seconds": 4.0,
    "//": "Re-quote less often; less chasing on every NBBO tick",
    "max_market_stale_seconds": 1200,
    "nbbo_coalesce_seconds": 6.0,
    "//": "Wait longer before acting on NBBO changes"
  },

  "risk": {
    "cancel_on_invalid_market": true,
    "require_live_data": false
  },

  "logging": {
    "log_file": "market_maker.log"
  },

  "diagnostics": {
    "log_bid_ask_ticks": false,
    "log_invalid_market_reasons": false,
    "nbbo_watchdog_seconds": 300,
    "watchdog_repeat_seconds": 600
  }
}
